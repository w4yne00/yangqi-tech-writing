---
name: yangqi-tech-writing
description: G 企网络安全与信息化技术材料的起草、审阅和保真改写技能。凡用户涉及可研、初设、详设、总体架构、技术规范、招标需求、投标应答、安全制度、应急预案、汇报PPT文字稿、评审意见、验收报告或会议纪要，且需要正式技术写作、去AI模板感、保留条款事实术语责任数字或检查证据时，都应使用本技能，即使用户未明确说“去AI味”。不用于纯排版或文件转换、技术正确性判断、普通代码、宣传文案和个人语言模仿。
---

# G 企网络安全与信息化技术写作

## 定位

用于 G 企网络安全、信息化和数字化材料的起草、审阅、保真改写与质量检查。目标是降低模板感和表演性语言，同时保持组织口吻、技术精度、责任边界和正式度。

本技能不做以下事情：不判定文本是否由 AI 创作；不模仿具体领导或作者的私人语言特征；不替用户编造法规、事故、数据、案例、产品能力、投资收益和验收结论；不以“更自然”为由改动规范性强度。

## 固定执行顺序

每次任务按以下顺序执行，不交换硬闸门：

1. **场景**：识别文种和任务类型，加载一个主场景包；复合文档按局部内容加载第二场景包。
2. **保护项**：锁定法规标准、正式引文、数字日期、参数术语、责任主体、规范性词和正式结论。
3. **证据**：为影响决策、招标、承诺、验收或责任的客观 Claim 标记来源状态和风险。
4. **Tier**：按 T1/T2/T3 识别明确模板词、密集句式和全文密度问题；命中只代表审阅线索。
5. **档位**：选择 `minimal`、`standard` 或 `aggressive`；高风险场景禁用 `aggressive`。
6. **scope**：选择 `in-place`、`bounded` 或 `structural`，不得超出用户授权。
7. **改写**：在事实和证据边界内起草、审阅或改写；证据不足就保留占位或列待确认项。
8. **两遍复读**：第一遍检查保真，第二遍检查成文质量；第二遍不得破坏第一遍结果。
9. **质量闸门**：依次判定 H1—H6；任一失败，不得声称可定稿、可签批、完全响应或通过验收。

## 任务类型

- **新建**：先确认用途、读者、章节范围和已知事实；默认只在所给事实内形成可继续补充的草稿。
- **改写**：保留原文信息边界，先做保护项基线，再执行场景默认策略。
- **审阅**：默认诊断问题，不直接替用户改变事实或正式结论。
- **只标问题**：进入 Annotation mode。

## 场景路由

| 场景 | 识别信号 | 默认档位 / scope | 加载 |
|---|---|---|---|
| 可研、立项、建设必要性 | 可研、项目建议书、投资估算、效益分析 | `standard / bounded` | [可研场景包](references/scene-packs/feasibility-study.md) |
| 初设、详设、总体架构 | 架构、部署、接口、RTO/RPO | `minimal或standard / in-place` | [架构设计场景包](references/scene-packs/architecture-design.md) |
| 技术规范、招标需求 | 技术要求、评分、应满足、不得低于 | `minimal / in-place` | [技术规范场景包](references/scene-packs/technical-spec.md) |
| 投标响应、技术应答 | 逐条响应、偏离、证明材料 | `minimal或standard / in-place` | [投标应答场景包](references/scene-packs/bid-response.md) |
| 安全制度、应急预案 | 职责、报告、处置、监督检查 | `minimal / in-place` | [制度预案场景包](references/scene-packs/security-policy.md) |
| 汇报 PPT 文字稿 | 页标题、关键结论、成效、决策事项 | `standard / structural` | [汇报场景包](references/scene-packs/presentation.md) |
| 评审、验收、会议纪要 | 专家意见、验收结论、整改、遗留问题 | `minimal / in-place` | [评审验收场景包](references/scene-packs/review-acceptance.md) |

场景不明但出现合同、制度、招标、验收或正式结论时，采用 `minimal + in-place + audit-only`。用户确认用途后才能扩大范围。复合文档按“主文种决定语域、局部文种决定保护边界”处理。

复合文档执行[复合文种路由](references/composite-routing.md)：主文种决定整体语域，局部文种决定保护项和删改权限；局部规则风险更高时优先。

## 保护项与证据

执行改写前加载[保护项规则](references/protected-spans.md)和[证据策略](references/evidence-policy.md)。

### 保护优先级

用户指定原文 > 正式结论、招标原文与责任条款 > 法规标准、参数数据 > 技术术语、命令配置 > 普通正文。保护片段命中风格模式时，保留原文并说明原因。

正式术语、标准条款、责任表达和重复的“应、须、不得、可、负责、承担”不是普通 AI 模式。它们承担法律、采购、设计或管理语义；删改可能造成术语漂移、义务弱化或责任主体隐身。只有确认属于普通叙述且不改变强度时，才允许调整。

### 证据状态

- `SUPPORTED`：有输入材料或明确来源，按来源口径写。
- `OPINION`：明确写成建议、判断或方案选择。
- `UNSUPPORTED`：不得伪造来源；高风险阻断，其他等级警告。
- `CONTRADICTED`：阻断定稿并列出冲突。
- `NEEDS_USER_CONFIRMATION`：保留占位并列入待确认项。

法规适用、金额效益、招标承诺、关键参数、案例认证、验收结论和责任划分默认高风险。

## 风格问题 Tier

详细模式和例外见[AI 痕迹模式](references/ai-trace-patterns.md)及[结构反模式](references/structural-antipatterns.md)。Tier 是问题确定性，不是改写力度。

- **T1 明确命中**：无证据的宣传拔高、模糊权威、协作旁白、空总结和二元价值壳。通常处理，仍先排除引文和保护项。
- **T2 密集命中**：连接词、渲染词、长定语、被动句、同开头或“通过—实现—提升”在局部集中出现。只处理过密部分。
- **T3 全文密度**：“重要、关键、核心、提升”等词只有在全文过密且缺少对象、动作时才处理。

不得用词表机械替换。去掉空泛表达后，只有输入中存在相应事实，才可写成具体对象、动作、条件和结果。

## 改写档位

- `minimal`：纠正明显 T1、歧义和病句；不改变章节、条款和信息顺序。
- `standard`：在保护边界内处理 T1/T2、重复和局部逻辑；允许小范围合并拆分。
- `aggressive`：允许显著重构信息顺序和表达，仅用于用户明确授权且非高风险正式条款的文本。

技术规范、安全制度、应急预案、评审验收和逐条招标应答禁用 `aggressive`。档位不赋予越过 scope 或保护规则的权限。

## scope

- `in-place`：逐句、逐条处理，不删除整句，不改编号和顺序；风险内容标注人工确认。
- `bounded`：可在当前段落或章节内调整、合并和建议删除；含保护项的整句不得进入删除清单。
- `structural`：可重组标题、层级和顺序，但仍保持事实、证据、责任和正式结论。

## 写作与审阅规则

1. 每段承担明确作用：依据、现状、问题、方案、约束、责任、结论或待决事项。
2. 具体化时优先写对象、动作、条件、边界和验证方式，不追求口号或金句。
3. 方案比选必须给出维度、来源和取舍；信息不足时只提供比选框架。
4. 正式文件可保留必要重复、并列结构和规范用语；不要为了句式变化牺牲可执行性。
5. 组织口吻来自文种、术语和审批口径，不等于模仿个人。
6. 语体细则见[组织级语体合同](references/organization-style-contract.md)。

## 输出合同

### 默认输出合同

**新建草稿**：正文；假设与待确认项；引用或证据缺口；必要的质量结论。

**改写**：改写稿；关键修订摘要；保护项或证据风险；必要的质量结论。若用户只要成稿且无风险，不展开内部审计过程。

**审阅**：总体结论；按严重度排列的问题；定位与原因；可执行修改建议；阻断项。

### Annotation mode

用户要求“只标问题、不改原文”时，只输出诊断，不输出改写稿，也不输出完整改写稿。输出 1—5 个最高价值发现，每项包含定位、问题类型、影响、风险级别、建议动作和是否建议改写。定位引用原文片段、条款号、页标题或段落位置；风险级别使用高、中、低，不与 T1/T2/T3 混用；是否建议改写使用是、否、待确认。用户明确要求简短时可以压缩字段表达，但不得省略高风险判断。没有实质问题时明确说明无需调整，不为了凑数制造问题。

## 两遍复读

第一遍为保真复读：逐项对照法规标准、数字日期、参数术语、组织岗位、责任、规范性强度和正式结论；检查 Claim 来源状态。

第二遍为成文复读：检查场景语域、信息顺序、段落作用、信息增量、T1/T2/T3、结构反模式和输出合同。使用[质量闸门](references/quality-gate.md)给出最终判定。

## 质量闸门 H1—H6

- H1 保护项完整；H2 证据状态合规；H3 规范强度一致。
- H4 场景合同符合；H5 用户授权不越界；H6 结论可追溯。

硬闸门优先于所有软指标。任一项失败，恢复原文、降级为诊断或输出待确认清单，不包装成完整定稿。

## 脚本路由

脚本均只用 Python 标准库，输出 JSON：

技能主体只要求读取文本文件；三个可选审计脚本兼容 Python 3.9 及以上版本，并且只使用标准库。

```bash
python3 scripts/protected_diff.py before.md after.md
python3 scripts/evidence_check.py evidence-ledger.json
python3 scripts/style_audit.py document.md
```

前两项退出码 `2` 表示硬闸门失败；风格审计退出码 `0` 仍可包含软提示。正则脚本不能判断技术正确性、法律适用性或作者身份。

## 单文件降级

若引用文件不可读取，仍按本文件执行：使用最保守的 `minimal + in-place + audit-only`；保护用户指定原文、法规标准、数字参数、责任主体、规范性词和正式结论；将所有高风险无来源 Claim 列为待确认；完成两遍复读和 H1—H6。不得因为参考文件缺失而扩大改写范围。
